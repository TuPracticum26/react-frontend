import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/versions/pendingReview')({
  component: RouteComponent,
})

function RouteComponent() {
  return <div>Hello "/versions/pendingReview"!</div>
}
